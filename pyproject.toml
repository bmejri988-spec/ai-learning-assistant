[project]
name = "ai-learning-assistant"
version = "0.1.0"
description = "AI Study Assistant project scaffold"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "pydantic",
    "python-dotenv",
]
